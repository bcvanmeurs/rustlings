// variables6.rs
//
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a
// hint.

// Constants are declared with the `const` keyword and must have a type declared.
const NUMBER: i32 = 3;
fn main() {
    println!("Number {}", NUMBER);
}
